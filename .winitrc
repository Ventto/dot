#!/bin/sh

if [ "$(tty)" = "/dev/tty1" ]; then
    # Brightness
    lux -S 60%

    # Audio
    amixer -q set Master mute

    # Keyboard
    export XKB_DEFAULT_LAYOUT=myus

    # Window manager
    exec sway -V
fi
